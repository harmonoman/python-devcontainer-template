[project]
name = "your-project-name"  # only thing a new project changes
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8.2",
  "ruff>=0.4"
]

[tool.uv]
package = false

[tool.ruff]
line-length = 88
